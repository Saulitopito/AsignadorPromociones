package jpa.promociones.procedimientos;

import javax.persistence.*;

import java.util.*;

import jpa.promociones.modelo.Clientes;

public class Procedimientos {

	/*EntityManagerFactory emf = Persistence.createEntityManagerFactory("UnidadPersonas");
    EntityManager em = emf.createEntityManager();
    List<Clientes> lista=em.createQuery("select p from Persona p", Clientes.class).getResultList();

    for (Clientes c:lista) {

        System.out.println(c.getNombre());
        for(Pagos p :c.getMora()) {

            System.out.println(l.getTitulo());
            System.out.println(l.getPaginas());

        }
    }*/
	
}
