import { Actividad } from './actividad';
import { Ciudad } from './ciudad';

export class ClientePromo {

    nombrePromoDTO: string;
    nombreDTO: string;
    ciudadDTO: Ciudad;
    actividadDTO: Actividad;
    porcentajeDTO: number;
    valorDTO: number;
    valorRealDTO: number;

}
